import { createPendingTopup, createPaypalOrderRecord, finalizePaypalCapture, markPaypalOrderApproved, markWebhookProcessed, recordWebhook, supabaseServerKey, type SupabaseAdminEnv } from "./paypal-payment-ledger";
export interface PayPalEnv extends SupabaseAdminEnv { PAYPAL_CLIENT_ID?:string; PAYPAL_CLIENT_SECRET?:string; PAYPAL_ENV?:string; PAYPAL_WEBHOOK_ID?:string; SUPABASE_PUBLISHABLE_KEY?:string; }
type JsonRecord=Record<string,unknown>; const MAX_BODY_BYTES=32*1024; const ALLOWED_CURRENCIES=/^[A-Z]{3}$/;
export async function handlePayPalRequest(request:Request,env:PayPalEnv):Promise<Response>{const url=new URL(request.url);if(request.method==="GET"&&url.pathname==="/v1/paypal/config")return json({ok:true,provider:"paypal",environment:paypalBaseUrl(env).includes("sandbox")?"sandbox":"live",configured:Boolean(env.PAYPAL_CLIENT_ID&&env.PAYPAL_CLIENT_SECRET&&supabaseServerKey(env))});if(url.pathname==="/v1/paypal/orders"&&request.method==="POST"){const i=await authenticate(request,env);if(!i)return json({error:"unauthorized"},401);return createOrder(request,env,i.userId);}const cm=url.pathname.match(/^\/v1\/paypal\/orders\/([^/]+)\/capture$/);if(cm&&request.method==="POST"){const i=await authenticate(request,env);if(!i)return json({error:"unauthorized"},401);return captureOrder(cm[1],env,i.userId);}const om=url.pathname.match(/^\/v1\/paypal\/orders\/([^/]+)$/);if(om&&request.method==="GET"){const i=await authenticate(request,env);if(!i)return json({error:"unauthorized"},401);return showOrder(om[1],env,i.userId);}if(url.pathname==="/v1/paypal/webhook"&&request.method==="POST")return verifyWebhook(request,env);return json({error:"paypal_route_not_found"},404);}
async function authenticate(request:Request,env:PayPalEnv):Promise<{userId:string}|null>{const auth=request.headers.get("Authorization")??"";if(!auth.startsWith("Bearer ")||!env.SUPABASE_URL||!env.SUPABASE_PUBLISHABLE_KEY)return null;const token=auth.slice(7).trim();if(!token||token.length>8192)return null;try{const r=await fetch(`${env.SUPABASE_URL}/auth/v1/user`,{headers:{Authorization:`Bearer ${token}`,apikey:env.SUPABASE_PUBLISHABLE_KEY}});if(!r.ok)return null;const u=await r.json() as {id?:string};return u.id&&/^[0-9a-f-]{36}$/i.test(u.id)?{userId:u.id}:null;}catch{return null;}}
async function createOrder(request:Request,env:PayPalEnv,userId:string):Promise<Response>{const b=await readJson(request);if(!b)return json({error:"invalid_json"},400);const amount=String(b.amount??"").trim(),currency=String(b.currency??"USD").trim().toUpperCase(),description=String(b.description??"TV49 East purchase").trim().slice(0,127),referenceId=String(b.reference_id??crypto.randomUUID()).trim().slice(0,256),returnUrl=String(b.return_url??"").trim(),cancelUrl=String(b.cancel_url??"").trim();if(!/^\d+(?:\.\d{1,2})?$/.test(amount)||Number(amount)<=0)return json({error:"invalid_amount"},400);if(!ALLOWED_CURRENCIES.test(currency))return json({error:"invalid_currency"},400);if(returnUrl&&!isHttpsUrl(returnUrl))return json({error:"return_url_must_be_https"},400);if(cancelUrl&&!isHttpsUrl(cancelUrl))return json({error:"cancel_url_must_be_https"},400);const amountCents=Math.round(Number(amount)*100),accessToken=await getAccessToken(env);if(!accessToken)return json({error:"paypal_not_configured"},503);const context:JsonRecord={shipping_preference:"NO_SHIPPING",user_action:"PAY_NOW"};if(returnUrl)context.return_url=returnUrl;if(cancelUrl)context.cancel_url=cancelUrl;const r=await paypalFetch(paypalBaseUrl(env),"/v2/checkout/orders",accessToken,{method:"POST",headers:{"PayPal-Request-Id":`tv49-${crypto.randomUUID()}`},body:JSON.stringify({intent:"CAPTURE",purchase_units:[{reference_id:referenceId,description,amount:{currency_code:currency,value:amount}}],application_context:context})});if(!r.ok)return paypalResponse(r);const p=await r.json() as JsonRecord,orderId=typeof p.id==="string"?p.id:"";if(!orderId)return json({error:"paypal_order_missing_id"},502);try{const tx=await createPendingTopup(env,userId,orderId,amountCents,currency);const links=Array.isArray(p.links)?p.links as JsonRecord[]:[];const approval=links.find(x=>x.rel==="payer-action"||x.rel==="approve");await createPaypalOrderRecord(env,userId,String(tx.id),orderId,amountCents,currency,typeof approval?.href==="string"?approval.href:undefined);}catch(error){return json({error:"payment_persistence_failed",detail:error instanceof Error?error.message:"unknown"},503);}return json(p);}
async function captureOrder(orderId:string,env:PayPalEnv,userId:string):Promise<Response>{if(!/^[A-Za-z0-9_-]{1,64}$/.test(orderId))return json({error:"invalid_order_id"},400);const lookup=await supabaseQuery(env,`/paypal_orders?order_id=eq.${encodeURIComponent(orderId)}&user_id=eq.${encodeURIComponent(userId)}&select=order_id&limit=1`);if(!lookup.ok)return json({error:"payment_lookup_failed"},503);const rows=await lookup.json() as unknown[];if(!rows.length)return json({error:"order_not_owned"},404);const token=await getAccessToken(env);if(!token)return json({error:"paypal_not_configured"},503);const r=await paypalFetch(paypalBaseUrl(env),`/v2/checkout/orders/${encodeURIComponent(orderId)}/capture`,token,{method:"POST",headers:{"PayPal-Request-Id":`tv49-capture-${orderId}`},body:"{}"});if(!r.ok)return paypalResponse(r);const p=await r.clone().json() as JsonRecord;const captureId=findCaptureId(p);if(captureId&&String(p.status??"").toUpperCase()==="COMPLETED")await finalizePaypalCapture(env,orderId,captureId);return paypalResponse(r);}
async function showOrder(orderId:string,env:PayPalEnv,userId:string):Promise<Response>{if(!/^[A-Za-z0-9_-]{1,64}$/.test(orderId))return json({error:"invalid_order_id"},400);const lookup=await supabaseQuery(env,`/paypal_orders?order_id=eq.${encodeURIComponent(orderId)}&user_id=eq.${encodeURIComponent(userId)}&select=order_id&limit=1`);if(!lookup.ok)return json({error:"payment_lookup_failed"},503);const rows=await lookup.json() as unknown[];if(!rows.length)return json({error:"order_not_owned"},404);const token=await getAccessToken(env);if(!token)return json({error:"paypal_not_configured"},503);return paypalResponse(await paypalFetch(paypalBaseUrl(env),`/v2/checkout/orders/${encodeURIComponent(orderId)}`,token));}
async function verifyWebhook(request:Request,env:PayPalEnv):Promise<Response>{if(!env.PAYPAL_WEBHOOK_ID)return json({error:"paypal_webhook_not_configured"},503);const raw=await request.text();if(new TextEncoder().encode(raw).byteLength>MAX_BODY_BYTES)return json({error:"webhook_body_too_large"},413);const tid=request.headers.get("paypal-transmission-id"),tt=request.headers.get("paypal-transmission-time"),cert=request.headers.get("paypal-cert-url"),algo=request.headers.get("paypal-auth-algo"),sig=request.headers.get("paypal-transmission-sig");if(!tid||!tt||!cert||!algo||!sig)return json({error:"missing_paypal_webhook_headers"},400);const token=await getAccessToken(env);if(!token)return json({error:"paypal_not_configured"},503);const event=safeJson(raw) as JsonRecord,eventId=typeof event.id==="string"?event.id:"",eventType=typeof event.event_type==="string"?event.event_type:"UNKNOWN";if(!eventId)return json({error:"missing_event_id"},400);const vr=await paypalFetch(paypalBaseUrl(env),"/v1/notifications/verify-webhook-signature",token,{method:"POST",body:JSON.stringify({auth_algo:algo,cert_url:cert,transmission_id:tid,transmission_sig:sig,transmission_time:tt,webhook_id:env.PAYPAL_WEBHOOK_ID,webhook_event:event})});if(!vr.ok)return paypalResponse(vr);const verification=await vr.json() as JsonRecord;if(verification.verification_status!=="SUCCESS")return json({error:"paypal_webhook_signature_invalid"},400);const orderId=extractOrderId(event),captureId=extractCaptureId(event);let inserted=false;try{inserted=await recordWebhook(env,{eventId,eventType,orderId,captureId,transmissionId:tid,payload:event});}catch(error){return json({error:"webhook_persistence_failed",detail:error instanceof Error?error.message:"unknown"},503);}if(!inserted)return json({ok:true,verified:true,duplicate:true,event_id:eventId,event_type:eventType});try{if(eventType==="CHECKOUT.ORDER.APPROVED"&&orderId)await markPaypalOrderApproved(env,orderId);if(eventType==="PAYMENT.CAPTURE.COMPLETED"&&orderId&&captureId)await finalizePaypalCapture(env,orderId,captureId);await markWebhookProcessed(env,eventId);}catch(error){const message=error instanceof Error?error.message:"unknown";await markWebhookProcessed(env,eventId,message).catch(()=>{});return json({error:"webhook_processing_failed"},503);}return json({ok:true,verified:true,processed:true,event_id:eventId,event_type:eventType});}
async function supabaseQuery(env:PayPalEnv,path:string,init:RequestInit={}):Promise<Response>{const key=supabaseServerKey(env);if(!key)throw new Error("missing_supabase_server_key");const h=new Headers(init.headers);h.set("apikey",key);h.set("Authorization",`Bearer ${key}`);h.set("content-type","application/json");return fetch(`${env.SUPABASE_URL}/rest/v1${path}`,{...init,headers:h});}
function extractOrderId(e:JsonRecord):string|undefined{const r=e.resource as JsonRecord|undefined;if(!r)return;const s=r.supplementary_data as JsonRecord|undefined,related=s?.related_ids as JsonRecord|undefined;if(typeof related?.order_id==="string")return related.order_id;if(String(e.event_type).startsWith("CHECKOUT.ORDER")&&typeof r.id==="string")return r.id;return;}
function extractCaptureId(e:JsonRecord):string|undefined{const r=e.resource as JsonRecord|undefined;return r&&String(e.event_type).startsWith("PAYMENT.CAPTURE")&&typeof r.id==="string"?r.id:undefined;}
function findCaptureId(o:JsonRecord):string|undefined{const units=Array.isArray(o.purchase_units)?o.purchase_units as JsonRecord[]:[];for(const u of units){const payments=u.payments as JsonRecord|undefined,caps=payments?.captures as JsonRecord[]|undefined;if(caps?.[0]&&typeof caps[0].id==="string")return caps[0].id;}return;}
async function getAccessToken(env:PayPalEnv):Promise<string|null>{if(!env.PAYPAL_CLIENT_ID||!env.PAYPAL_CLIENT_SECRET)return null;const r=await fetch(`${paypalBaseUrl(env)}/v1/oauth2/token`,{method:"POST",headers:{Authorization:`Basic ${btoa(env.PAYPAL_CLIENT_ID+":"+env.PAYPAL_CLIENT_SECRET)}`,"Content-Type":"application/x-www-form-urlencoded",Accept:"application/json"},body:"grant_type=client_credentials"});if(!r.ok)return null;const d=await r.json() as {access_token?:string};return typeof d.access_token==="string"?d.access_token:null;}
function paypalBaseUrl(env:PayPalEnv):string{const mode=String(env.PAYPAL_ENV??"sandbox").toLowerCase();return mode==="live"||mode==="production"?"https://api-m.paypal.com":"https://api-m.sandbox.paypal.com";}
async function paypalFetch(baseUrl:string,path:string,token:string,init:RequestInit={}):Promise<Response>{const h=new Headers(init.headers);h.set("Authorization",`Bearer ${token}`);h.set("Accept","application/json");if(init.body&&!h.has("Content-Type"))h.set("Content-Type","application/json");return fetch(`${baseUrl}${path}`,{...init,headers:h});}
function isHttpsUrl(v:string):boolean{try{return new URL(v).protocol==="https:";}catch{return false;}}
async function readJson(r:Request):Promise<JsonRecord|null>{const length=Number(r.headers.get("content-length")??"0");if(length>MAX_BODY_BYTES)return null;try{const v=await r.json();return v&&typeof v==="object"&&!Array.isArray(v)?v as JsonRecord:null;}catch{return null;}}
function safeJson(raw:string):unknown{try{return JSON.parse(raw);}catch{return {};}}
async function paypalResponse(r:Response):Promise<Response>{const body=await r.text();return new Response(body||"{}",{status:r.status,headers:{"content-type":r.headers.get("content-type")??"application/json; charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff"}});}
function json(v:unknown,status=200):Response{return new Response(JSON.stringify(v),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff"}});}
