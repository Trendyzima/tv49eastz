# Integrated streaming components

This directory is reserved for the three integrated upstream components:

- `player/` — Android TV/IPTV playback client
- `channel-engine/` — live-channel scheduling, playout and transcoding services
- `media-router/` — live media relay/proxy service

The components are intended to be maintained as part of the `tv49eastz` project rather than as standalone branch names.
